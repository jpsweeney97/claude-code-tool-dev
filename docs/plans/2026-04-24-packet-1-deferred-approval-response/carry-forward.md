# Packet 1 Carry-Forward Tracker

Running list of deferred items discovered during execution of Packet 1 (T-20260423-02). Items are appended as they're found and removed when resolved. Intent: prevent indefinite accumulation (handoff Risk: "By Task 22 we may have 30+ deferred items").

**Scope:** Minor findings that are non-blocking for the current task but should be addressed at an appropriate later point. Critical and Important findings are fixed in-scope, never carried forward.

**When to sweep:** End-of-phase polish, or when a later task naturally touches the same area.

---

## Open items

### From Phase A (originally in prior handoff)

| # | Item | Lands at / How to resolve | Source |
|---|---|---|---|
| A4 | Unused `import pytest` in Task 3 and Task 4 test files | End-of-phase polish (below threshold per user policy) | Phase A code review |
| A5 | Class-level annotation style on `DelegationStartError` (`reason: str`, `cause: Exception \| None`) is style-divergent from `replay.py`'s exception classes | Design discussion at end-of-phase | Phase A code review |

### From Phase B Task 6

| # | Item | Lands at / How to resolve | Source |
|---|---|---|---|
| B6.1 | `test_has_resolution_action_field` is redundant with `test_default_values_are_safe` — both assert `req.resolution_action is None` on a default-constructed instance. Consider removing or repurposing to test a non-default value | End-of-phase polish | Task 6 code quality review |
| B6.2 | Inline `import json` inside `test_existing_records_replay_cleanly_with_none_defaults` and `test_new_fields_survive_update_status_roundtrip` — style-divergent from module-level imports used elsewhere in the file | End-of-phase polish | Task 6 code quality review |

### From Phase B Task 7

| # | Item | Lands at / How to resolve | Source |
|---|---|---|---|
| B7.1 | Unused `import pytest` in `test_pending_request_store_mutators.py` (no `pytest.*` symbol used in file) — same pattern as A4 | End-of-phase polish | Task 7 code quality review |
| B7.2 | Variable name inconsistency in `_replay`: `update_status` branch uses `req_id`; the 3 new branches (`mark_resolved`, `record_response_dispatch`, `record_protocol_echo`) use `rid`. Normalize on a future polish pass | End-of-phase polish | Task 7 code quality review |

### From Phase B Task 8

| # | Item | Lands at / How to resolve | Source |
|---|---|---|---|
| B8.1 | Style asymmetry between `record_response_dispatch` replay (line 294, reads `dispatch_result=record.get(...)`) and `record_dispatch_failure` replay (line 327, hardcodes `dispatch_result="failed"`). Both mutators write the value unconditionally, so behavior matches under normal flow; under hand-edit JSONL corruption the read-path drifts to `None` while the hardcode stays `"failed"`. Tautological-but-safe; pick one style on a future pass. | End-of-phase polish | Task 8 code quality review |
| B8.2 | No explicit reopen round-trip tests for `record_timeout` and `record_dispatch_failure` in `test_pending_request_store_atomic_mutators.py`. Only `test_record_internal_abort_round_trip_via_replay` instantiates a fresh `PendingRequestStore` to force file-open replay; the other two paths exercise replay via same-instance `store.get()`. Same `_replay()` code path; gap is the `__init__` (mkdir + store_path) coverage, already exercised by the abort round-trip. | End-of-phase test polish | Task 8 code quality review |

### From Phase C Task 10

| # | Item | Lands at / How to resolve | Source |
|---|---|---|---|
| C10.2 | New recovery test `test_recover_startup_closes_orphaned_none_decision_intent` (`tests/test_delegation_controller.py:2186`) omits the `journal.list_unresolved(session_id="sess-1") == []` post-recovery assertion present in the sibling test at `:2129`. Production code already satisfies it; gap is sibling-parity only. | End-of-phase test parity polish | Task 10 code quality review (F2) |
| C10.3 | Same new test also omits the `job.status == "unknown"` and `handle.status == "unknown"` assertions present in the sibling at `:2129`. The orphaned-active-job reconciliation block runs unconditionally for both `decision="approve"` and `decision=None` cases, so transition behavior is covered by the sibling — gap is sibling-parity only. | End-of-phase test parity polish | Task 10 code quality review (F3) |

### From Phase E Task 13

| # | Item | Lands at / How to resolve | Source |
|---|---|---|---|
| E13.1 | Redundant shape test at `tests/test_models_r2.py:287-300` — superseded by `tests/test_delegation_decision_result_shape.py` (which uses `dataclasses.fields()` + explicit `assert not hasattr` for each removed field, strictly stronger). Two assertions of the same invariant in two places. | End-of-Packet-1 polish — Task 14 W7 confirmed scope was helper-only and did not touch `test_models_r2.py` | Task 13 code quality review (Minor 3) |
| E13.2 | Docstring "Breaking change. Callers that previously read `pending_escalation`/`agent_context` from decide's response must switch to poll()" paragraph in `DelegationDecisionResult` is migration commentary that belongs in CHANGELOG / contracts.md, not a frozen dataclass docstring (becomes evergreen historical noise once Phase H lands). | Phase H — trim docstring back to one-sentence current-contract description; defer breaking-change framing to contracts.md | Task 13 code quality review (Minor 4) |
| E13.3 | `decision_accepted` is constant `True` at every controller construction site (rejection path returns `DecisionRejectedResponse` instead). The boolean serves as a wire-contract marker for JSON consumers to disambiguate from `DecisionRejectedResponse`'s shape, not a controller-varied discriminator. A one-line comment near the dataclass declaration would help future readers. | Phase H — alongside contracts.md updates | Task 13 code quality review (Minor 6) |

### From Phase E Task 14

| # | Item | Lands at / How to resolve | Source |
|---|---|---|---|
| E14.1 | `_ESCALATABLE_REQUEST_KINDS` (`delegation_controller.py:113`) and `EscalatableRequestKind` Literal (`models.py:23-27`) define the same three strings in two places. `frozenset(typing.get_args(EscalatableRequestKind))` would be a single-source-of-truth derivation. The new `test_escalatable_set_has_three_literals` test would catch a drift, so this is style/declarativeness, not correctness. | End-of-Packet-1 polish — declarative refactor; defer unless a fourth `EscalatableRequestKind` literal is added | Task 14 code quality review (Minor 4) |

### From Phase F Task 16

_(All items closed — see Closed items section below.)_

### From Phase G Task 17

| # | Item | Lands at / How to resolve | Source |
|---|---|---|---|
| G17.1 | **[Closed by Task 18 — see "From Phase G Task 18 + closeouts" closeout entry for full narrative.]** All 9 G17.1 tests dispositioned in Task 18: 3 deleted (obsolete CDFE per L11; CDFE class preserved) + 6 reclassified to G18.1 (per L12; finalizer-dependent — Phase H Task 19 owner). 0 items un-skipped. Lineage continues via DELETE notes in F16.2 + the new G18.1 entry below. | **Resolved by Task 18 (3 delete + 6 reclassify-to-G18.1)** | Task 17 carry-forward (F16.2 split + L13 reclassification); Task 18 L11 + L12 dispositions |
| G18.1 | **[Closed by Task 19 `1f97b333` (2026-04-27).]** All 6 finalizer-dependent skip decorators removed + body rewrites landed in the Task 19 feat commit. `_TASK_19_FINALIZER_GUARD_REASON` constant deleted from both test files (grep count = 0). `:1881` renamed to `test_decide_deny_marks_job_completed_and_closes_runtime` per pre-authorized deny semantics correction. All 6 tests pass under the new Captured-Request Terminal Guard. | **Resolved by Task 19 feat `1f97b333`** | Task 18 L12 reclassification + Task 19 L7 same-commit closure |
| TT.1 | `_FakeControlPlane` Pyright issues at `tests/test_delegation_controller.py:259, 2878, 3075, 3205, 3429, 3786` — instantiation sites use the test fake without proper Pyright-narrowable types in spots where the production type signature expects a different protocol. Pre-existing across feat (`2606fb03`) + fix (`b8e7f9ce`) chain; surfaced during Task 18 review. Not introduced by Task 18; recorded for end-of-Phase-G or end-of-Packet-1 typing polish alongside RT.1. | End-of-Phase-G or end-of-Packet-1 typing polish (alongside RT.1) | Task 18 dispatch packet (TT.1 nomination) + diagnostic surface |
| RT.1 | Pre-existing Pyright `TurnStatus` literal narrowing error at `packages/plugins/codex-collaboration/server/runtime.py:270` — pre-dates Phase F (noted in Task 16 G34 + Task 17 W10 as out-of-scope). Diagnostic remains across the chain `f3cfa61a → 8dd15971 → dc90c1d9 → 2606fb03 → b8e7f9ce`. Not introduced by Task 17 or Task 18; explicitly preserved (W10 + W19 equivalent forbade `runtime.py` touches as side-effect). | End-of-Phase-G or end-of-Packet-1 typing polish | Task 16 G34 + Task 17 W10 + dispatch-packet RT.1 nomination |

---

## Closed items

_(Move items here when resolved, with the commit SHA that resolved them)_

### From Phase B Task 6

- **[Resolved `3fbba140`]** `update_status` replay branch silently dropped 11 new fields. Originally triaged as carry-forward; code quality review upgraded to Critical-in-Task-6-scope. Fixed via `dataclasses.replace` + round-trip test.

### From Phase B Task 7

- **[Resolved `b623548b`]** `record_protocol_echo` replay branch crashed all store reads on `protocol_echo_signals: null` JSONL records (`tuple(None)` raises `TypeError`). Code quality reviewer flagged as Important I-1; fixed in-scope per Phase A protocol via `record.get(...) or ()` plus null-injection regression test.

### From Phase B Task 9 + closeout

- **[Resolved `c6bf834c`]** `DelegationJobStore._replay` used `asdict(existing)` + dict-spread, recursively coercing `artifact_paths` from `tuple` to `list` before reconstructing `DelegationJob`. Python doesn't runtime-validate generic types, so the dataclass silently accepted the list, violating the `tuple[str, ...]` field contract on every normal-path `update_status` / `update_status_and_promotion` / `update_promotion_state` replay. Symptom was bounded — one spurious `update_artifacts` write per poll at `delegation_controller.py:988` — but the type-contract violation was load-bearing for Phase B's replay-preservation principle. Discovered by Task 9 code-quality reviewer (P1, Important — pre-existing). Fixed in Phase B closeout by migrating all five replay branches to `dataclasses.replace()`, plus three regression tests asserting `type(retrieved.artifact_paths) is tuple` after each affected branch's replay. Same closeout commit also bundled Task 9 cleanup: `jid` → `job_id` rename and `isinstance(job_id, str)` guard for symmetry with the migrated branches (covers what would otherwise have been B9.1/B9.2/B9.3 carry-forward items).

### From Phase C Task 10 + closeout

- **[Resolved `9061d268`]** `OperationJournalEntry.completion_origin` Literal field landed in Task 10 (`e8786626`) without symmetric validator coverage in `_journal_callback` — the validator already enforced type + membership for `operation` (via `_VALID_OPERATIONS`) and `phase` (via `_VALID_PHASES`), but the new optional Literal field was uncovered. Surfaced by Task 10 code-quality review (F1, Minor by symptom — only hand-crafted JSONL triggers it). User escalated via Path A1 of a structured 3-axis disposition decision: principle-coherence at the Phase C boundary requires complete validation of the new schema field, not just type half. Closed in Phase C closeout by adding `"completion_origin"` to `_JOURNAL_OPTIONAL_STR` (type half, surfaces non-string values via the existing optional-string-type-check loop) plus a new `_VALID_COMPLETION_ORIGINS` frozenset and dedicated membership-check block in `_journal_callback` (membership half, surfaces non-Literal-value strings), matching the existing `operation`/`phase` validation pattern. Two new regression tests (`test_completion_origin_unknown_string_value_rejected` + `test_completion_origin_non_string_rejected`) confirm both branches surface through `check_health()` schema_violations diagnostics.

### From Phase D Task 12 + closeout

- **[Resolved `becbb124`]** Phase D Task 12 (`6aa623c0`) added the per-job capture-ready channel to `ResolutionRegistry` — `ParkedCaptureResult` 5-variant sum (`Parked` / `TurnCompletedWithoutCapture` / `TurnTerminalWithoutEscalation` / `WorkerFailed` / `StartWaitElapsed`), four worker-side `announce_*` methods, `_deliver_capture_outcome` helper, `wait_for_parked` main-thread blocker. Per spec §Capture-ready handshake and user disposition O1=(b), `wait_for_parked` owns channel lifecycle by popping `_capture_channels[job_id]` under the registry lock before returning; late `announce_*` calls observe `channel is None` in `_deliver_capture_outcome` and warn-no-op without raising. Quality review surfaced four Minor findings, all addressed in the bundled closeout fix:
  - **F1 (log-level convention symmetry):** `_deliver_capture_outcome` logged late capture-ready signal discards at `logger.info`; Task 11's `commit_signal` logs the analogous stale-token discard at `logger.warning`. Promoted to `logger.warning` for symmetry — late lifecycle anomalies now surface in default warning-level streams (operationally meaningful given the documented late-vs-timeout race window).
  - **F2 (threading-test idiom parity with Task 11):** four threading tests (`test_announce_turn_completed_empty_surfaces_variant`, `test_announce_turn_terminal_without_escalation`, `test_announce_worker_failed_surfaces_exception`, `test_capture_ready_channels_are_per_job`) called `t.join(timeout=...)` and proceeded directly to result-list assertions without first asserting `not t.is_alive()`. Task 11's `test_resolution_registry_per_request.py` uses the assertion uniformly. Five assertion lines added (the per-job test has two threads); avoids silent false positives where a deadlocked thread leaves an empty result list and the test fails downstream for the wrong reason.
  - **F3 (public-error-surface coverage):** no test exercised the `wait_for_parked` duplicate-job_id `RuntimeError` raise path. Added `test_wait_for_parked_duplicate_job_id_raises` (10 lines) using `pytest.raises(RuntimeError, match="wait_for_parked failed: duplicate")`; spawns a first-wait thread to establish the channel, asserts the second wait raises, then cleans up via `announce_turn_completed_empty` to avoid hanging the test suite.
  - **F4 (defensive-branch comment quality):** the `if channel_now is None:` early-return in `wait_for_parked`'s second `with self._lock:` block is unreachable under current code (nothing else pops `_capture_channels` between the method's two lock acquisitions). Coordinator chose option (a) — strengthen the comment with future-proofing rationale (defensive against future admin path discard like a `discard_capture_channel(job_id)` helper that could race with `wait_for_parked`) — over option (b) `assert False` to preserve the registry's "tolerate surprising state, never deadlock or raise" posture. Branch retained as defensive early-return.

  One additional observation surfaced in review (NOT a defect, recorded for future readers): after pop-on-return (O1=(b) lifecycle), the `_CaptureReadyChannel.resolved` flag is no longer externally observable — it serves only as an internal idempotency guard inside `_deliver_capture_outcome` to prevent double-delivery within a single channel's lifetime. Plan-literal preservation; safe under O1=(b). Worth a one-line comment if a future Phase touches the channel internals.

### From Phase D Task 11 + closeout

- **[Resolved `8d4f9b96`]** Phase D Task 11 (`55444d94`) introduced `ResolutionRegistry` (in-memory cross-thread coordination primitive, IO-2). Quality review surfaced four Minor findings, all addressed in the bundled closeout fix:
  - **F1 (error-format convention):** `register()` duplicate-request_id error message used `"ResolutionRegistry.register: duplicate ..."` instead of the project `"{operation} failed: {reason}. Got: ..."` convention from `~/.claude/CLAUDE.md`. Normalized to `"ResolutionRegistry.register failed: duplicate request_id..."`.
  - **F2 (test-intent confusion):** `test_late_timer_against_decided_entry_is_noop` asserted `"r1" in reg._entries` after `commit_signal()` — the assertion was valid only because the test omitted `discard()`, conflating "timer is no-op" with "entry persists by contract" (the latter is not a documented property). Walked back the internal-table assertion; rewrote test to spawn a worker thread and observe the contract surface directly: worker receives operator's resolution exactly once (no double-wake from the would-be late timer), entry stays in consuming state.
  - **F3 (latent resource-leak risk):** `wait()` docstring did not document the caller's obligation to call `discard(request_id)` after consuming the returned resolution. Without that call, the entry persists in the registry indefinitely. Added the caller-obligation sentence to the docstring; production callsite contract is now self-documenting.
  - **F4 (advertised-but-inert protection):** `ReservationToken.generation` was initialized to `0` in `register()` and never incremented, leaving the docstring's "stale tokens (entry discarded and re-registered) cannot commit_signal into a different entry's state" claim unfulfilled — both old and new tokens for a recycled `request_id` would carry `generation=0`, the guard `entry.generation != token.generation` would not fire, and (under a sufficiently narrow exploit path) the old token's `commit_signal` could pollute the new entry's state. Coordinator initially recommended Path B (docstring-only retreat citing spec §1216's "`reserve()` is sole coordination primitive"); user escalated to Path D (complete fix) on the principle that §1216 governs stale TIMER races specifically, not the broader stale-token-after-discard-re-register contract that the `ReservationToken` docstring already advertises. Closeout adds `_next_generation` registry-instance counter (initialized 0, monotonically incremented under the registry lock per `register()`), threads it into `_RegistryEntry.generation`, and tightens the `ReservationToken` docstring to accurately describe the now-real protection scope. New regression test `test_stale_token_after_discard_and_reregister_is_rejected` pins the protection directly via a 7-step exploit-path observation: register → reserve → discard → re-register → reserve new → stale commit on old token → assert worker still blocked + new entry uncommitted → commit new token → assert worker receives new resolution. Generation counter is now load-bearing (not defensive cruft); the docstring claim and the implementation match.

### From Phase E Task 13 + closeouts

- **[Resolved `4ed54097` + closeouts `7b85b503` / `1187e8c4` / `da46efa9` / `e9955ec7`]** Phase E Task 13 rewrote `DelegationDecisionResult` from the pre-Packet-1 5-field shape (`job` / `decision` / `resumed` / `pending_escalation` / `agent_context`) to the new 3-field shape (`decision_accepted` / `job_id` / `request_id`), deleted the custom `DelegationDecisionResult` serializer branch in `mcp_server.py` (with its orphan local import), rewrote the `decide()` success construction sites, migrated four controller-level tests to `controller.poll()` with proper `DelegationPollResult` narrowing, and migrated three MCP-level integration tests to `codex.delegate.poll`. Wire-shape-only Phase E slice — `decide()` remains synchronous internally during the Phase G interregnum (per L7 lock; Phase G Task 18 owns the async control-flow rewrite). Suite: 974 → 978 passing (4 new tests in `tests/test_delegation_decision_result_shape.py`).

  Five-commit chain (canonical 3-commit shape stretched by iterative review surfacing the same defect-class on both arms separately):
  - **`4ed54097` (feat):** primary rewrite — `DelegationDecisionResult` 3-field shape + shape-only docstring; MCP `codex.delegate.decide` serializer branch deleted (custom `isinstance(result, DelegationDecisionResult)` payload-builder removed; `return asdict(result)` fallthrough now handles the new shape, symmetric with `poll`/`promote`/`discard`); function-local `from .models import DelegationDecisionResult` import removed (N1); three `decide()` construction sites rewritten (deny + approve+escalation + approve+completed); per-test triage applied to test migrations (no blanket `result.X` → `poll().X` substitution; no `# Phase G:` deferral comments needed, since all migrated assertions were observable state, not timing-bound).
  - **`7b85b503` (closeout 1):** dropped dead `updated_job =` binding in deny path (`delegation_controller.py:1822` pre-rewrite). The pre-Packet-1 shape passed `job=updated_job`; after the 3-field rewrite consumed only `job_id` (already in scope as parameter), the assignment became orphaned. The `self._persist_job_transition(job_id, "failed")` call itself is preserved for its side-effect (persists "failed" transition); only the variable binding was removed.
  - **`1187e8c4` (closeout 2):** added `assert isinstance(poll, DelegationPollResult)` narrowing in four migrated controller tests (`test_decide_approve_resumes_runtime_and_returns_completed_result`, `test_decide_approve_can_reescalate_with_new_pending_request`, `test_decide_deny_marks_job_failed_and_closes_runtime`, `test_decide_rejects_stale_request_id_after_reescalation`). `controller.poll()` returns `DelegationPollResult | PollRejectedResponse`; direct `poll.X` attribute access requires union narrowing under Pyright. Pattern matches the package's existing idiom at `tests/test_delegation_controller.py:1705` (decide-result narrowing) and `:2514` (poll-result narrowing). Tests passed at runtime because `poll` always resolved to the success branch in those scenarios, but type-system hygiene required the assert.
  - **`da46efa9` (closeout 3):** collapsed vestigial `if isinstance(follow_up, DelegationEscalation):` branch in `decide()` approve path. Both arms returned literally identical `DelegationDecisionResult(decision_accepted=True, job_id=job_id, request_id=request_id)` post-rewrite — the conditional only existed because the pre-Packet-1 shape extracted `follow_up.pending_escalation` / `follow_up.agent_context` per-branch. Also dropped the orphan `follow_up =` binding from `self._execute_live_turn(...)` (the call still executes for its journaling side-effects). Symmetric counterpart to closeout-1's `updated_job` cleanup — same defect class on the opposite arm.
  - **`e9955ec7` (closeout 4):** folded misnamed `tests/test_mcp_decide_response_shape_integration.py` (single test, just called `asdict()` on a freshly-constructed dataclass — did NOT exercise the MCP boundary as the `_integration.py` filename claimed) into `tests/test_delegation_decision_result_shape.py` as `test_asdict_emits_exactly_three_keys`, strengthening the assertion (now also asserts each value, not just the key set). Deleted the misnamed file. Genuine MCP-boundary coverage of the new shape is preserved at `tests/test_mcp_server.py:1192-1199` (drives `handle_request`) and `tests/test_delegate_start_integration.py:898+` (full e2e for approve / deny / re-escalation). The `_integration.py` suffix is a load-bearing convention in this package for tests that drive `server.handle_request(...)` end-to-end; misnaming a non-integration test diluted the discriminator.

  **Defect-class pattern across closeouts 1 + 3:** the pre-Packet-1 5-field shape extracted attributes from per-arm bindings (`updated_job.status` for deny, `follow_up.pending_escalation`/`.agent_context` for approve+escalation). After the 3-field shape consumed only `job_id` and `request_id` (already in scope as `decide()` parameters), every binding/branch that existed solely to feed the constructor became orphaned. Both arms surfaced this in their own closeout commits — implementer caught the deny-arm version in self-review territory after the controller's first-round diagnostics review; the approve-arm version (vestigial `isinstance` branch + `follow_up =` binding) required code-quality review to surface. A single up-front "what was this binding/branch for?" pass during the feat commit would have caught both.

  **Lock conformance throughout:** L1 (3-field shape), L2 (shape-only docstring), L3 (custom serializer branch deleted, not replaced), L4 (success construction sites use only the 3 new fields — closeout-3 collapsed three sites into two, descriptive count was incidental), L5 (no `completion_origin` co-edits in `decide()` — C10.4 deferred to Phase F), L6 (no `contracts.md` edits — Phase H), L7 (no `ResolutionRegistry` wiring in `decide()` — Phase G Task 18). Spec compliance review confirmed all locks honored; code quality review surfaced 2 Important findings (closeouts 3 + 4) and 4 Minor findings (3 deferred per E13.1-E13.3 above; 1 declined as intentional convention preservation — repeated `# Post-dispatch ... (Packet 1)` comments kept per-site for grep-ability, matching the package's existing `# C10.4:` / `# Phase G:` style markers).

### From Phase F Task 15 + closeout

- **[Resolved `94c4dab7` + closeout `6f23b745`]** Phase F Task 15 landed the worker-thread scaffold: new `packages/plugins/codex-collaboration/server/worker_runner.py` (116 lines post-closeout) defines `_WorkerRunner` class + `spawn_worker` helper; `_execute_live_turn` gained a new `except _WorkerTerminalBranchSignal as signal:` clause inserted BEFORE the existing generic `except Exception:` in the first `run_execution_turn` try block, mapping pre-capture sentinel reason `"unknown_kind_interrupt_transport_failure"` to `DelegationStartError(reason=..., cause=None)` and post-branch reasons to `return self._job_store.get(job_id)` (with plan-literal `assert stored is not None, ...` invariant); `_load_or_materialize_inspection`'s terminal-status tuple expanded to `("completed", "failed", "canceled", "unknown")` with explicit `if job.status == "canceled": return None` short-circuit BEFORE `load_snapshot`. Scaffold-only — `_WorkerRunner` not yet wired into production paths (Task 17), sentinel catch is structurally dead code under Task 15 scope (Task 16 lands the 6 raise sites). Suite: 978 → 983 passing (5 net new tests in `tests/test_worker_runner.py` — 1 smoke + 2 contract guards on canceled-inspection + 2 sentinel-catch tests using `monkeypatch` + `caplog` + `MagicMock(spec=type(controller._artifact_store))`); 8 still skipped (Task 14's Mode A + Mode B set unchanged). W8 invariant holds: `grep -nF "_WorkerTerminalBranchSignal(reason=" delegation_controller.py | wc -l` returns 0 in production code.

  **A1 closed by L4:** the carry-forward A1 trap (`_WorkerTerminalBranchSignal` is `@dataclass(frozen=True) Exception` with empty `args`, so `str(signal) == ""`) is closed by the catch site logging `signal.reason` (not `str(signal)`) at `logger.info` severity. Severity choice: post-branch sentinel handling is expected control flow under Task 16+, so `info` is correct (warning/error would pollute streams). Logger reused from existing module-level `logger = logging.getLogger(__name__)` at `delegation_controller.py:104`.

  **Two-commit chain (canonical `feat + closeout-fix` shape; closeout-docs is this entry):**
  - **`94c4dab7` (feat):** primary scaffold — `worker_runner.py` (NEW), `_execute_live_turn` sentinel catch (delegation_controller.py:845-871), `_load_or_materialize_inspection` tuple+early-return (delegation_controller.py:1042-1049), `test_worker_runner.py` (NEW). Plan-template lint hygiene: dropped unused `Callable` import + dead `result =` binding from worker_runner.py per L2.
  - **`6f23b745` (closeout-fix):** code-quality review surfaced 3 Important + 2 Minor cleanups, all addressed in one commit. Important: removed dead `controller` parameter from `_make_running_job` test helper + both call sites (lines 28, 145, 217); underscored `control_plane` → `_control_plane` at both 8-tuple unpacks (lines 129, 202) for consistency with sibling `test_delegation_controller.py` convention; removed unused `monkeypatch: pytest.MonkeyPatch` parameter from pre-capture sentinel test (copy-paste artifact from post-branch test — pre-capture test injects sentinel via `mock_session.run_execution_turn.side_effect`, not `monkeypatch.setattr`). Minor: log format `reason=%s` → `reason=%r` for project convention consistency with `job_id=%r` and matching `%r` usage at lines 144, 779, 967; `worker_runner.py` module docstring 2nd bullet rewritten to match `run()`'s actual fallthrough behavior (was misleading: claimed "translates a normal return" but `run()` discards the return value and unconditionally calls `announce_turn_completed_empty`).

  **Convergence map drove dispatch:** the binding `task-15-convergence-map.md` working doc (8 locks + 11 watchpoints + 5-test triage table + structural-not-line-numbered acceptance criteria) was drafted via the user's two-read protocol (controller's read + user's `/copy`-output independent read converged). Saved as a durable working artifact (Phase E precedent), referenced by handoff Next Steps, then loaded by fresh dispatch session. Pattern locked-in for Task 16+: convergence-map-as-working-doc decouples binding scope contracts from session boundaries.

  **Lock conformance throughout:** L1 (scaffold-only — no raise sites, no handler rewrite, no `start()`/`__init__`/`decide()`/`poll()` changes), L2 (worker_runner.py lint-clean — `Callable` removed, `result =` elided), L3 (sentinel catch BEFORE generic `except Exception` in first try block; second try block around `_finalize_turn` untouched), L4 (`signal.reason` logged at `logger.info` via existing module logger — closes A1), L5 (pre-capture maps to `DelegationStartError(reason=..., cause=None)` literal — no `from` chain), L6 (post-branch returns stored job with plan-literal `assert stored is not None` invariant honored), L7 (canceled added to terminal tuple AND explicit early-return BEFORE `load_snapshot`), L8 (tests use module-local `_build_controller` + built-in `tmp_path`/`monkeypatch`/`caplog` + `unittest.mock.MagicMock`/`patch.object` — no fictional fixture names). Watchpoints W1-W11 all respected; spec compliance review independently verified each lock and watchpoint against commit `94c4dab7`. Code quality reviewer found cleanups but no spec violations; closeout-fix landed all Important + Minor items in one commit.

  **No new carry-forward items emerged from Task 15.** Pre-existing pyright diagnostics noted but explicitly out of scope: `_sanitize_error_string` unused at `delegation_controller.py:121` (confirmed pre-existing at baseline `d8f1911b`); pyright environment config issue on `pytest` import at `tests/test_worker_runner.py:9` (runtime suite passes, same class as the existing `# type: ignore[import]` on `_build_controller` import); pyright `reportUnusedVariable` flagging underscore-prefixed test-helper unpacks like `_worktree_manager`/`_lineage`/`_journal`/`_pending` (project pyright config issue — these follow Python convention for intentional non-use, but pyright doesn't auto-respect underscore-prefix without `reportUnusedVariable = "none"` config; not Task 15's responsibility to address).

### From Phase E Task 14 + closeouts

- **[Resolved `becfc316` + closeouts `65f270ab` / `b66d838f`]** Phase E Task 14 rewrote the projection helpers in `delegation_controller.py`: `_project_request_to_view` now raises `UnknownKindInEscalationProjection` for non-escalatable kinds (belt-and-suspenders runtime guard against `EscalatableRequestKind` Literal violations); `_project_pending_escalation` switched from collaboration-anchored (`(collaboration_id: str)`) to job-anchored (`(job: DelegationJob)`) signature with terminal/unparked/tombstone guards. Added `_ESCALATABLE_REQUEST_KINDS: frozenset[str]` module-level constant. Updated single production callsite in `poll()` (now passes `refreshed`, not `refreshed.collaboration_id`). Added new `tests/test_projection_helpers.py` with 8 helper-isolated unit tests using module-local helpers (no conftest factory fixtures, per W4 lock against fabricated precedent). Wire-shape-only Phase E slice — helpers stay pure; abort-signaling and exception-catch logic owned by callsites (Task 17 for `start()`, Task 20 for `poll()`). Suite: 978 passing → 978 passing + 8 newly skipped (per below).

  Three-commit chain (canonical `feat + N closeout-fixes + closeout-docs` shape; closeouts driven by Pyright hygiene + code-quality review):
  - **`becfc316` (feat):** primary rewrite — both helpers + module constant + single callsite update + new test file. 8 pre-existing tests dispositioned via `@pytest.mark.skip` decorators (substantive Phase G/F citations) due to convergence-map L4 transitive-effects gap (see below).
  - **`65f270ab` (closeout 1):** Pyright closeout. Added `cast(EscalatableRequestKind, request.kind)` at the construction site in `_project_request_to_view` (`delegation_controller.py:985`) — the runtime guard at `:975-981` ensures only escalatable kinds reach this point, but Pyright cannot narrow `request.kind` (still typed as `PendingRequestKind` 4-literal) to the `EscalatableRequestKind` 3-literal expected by `PendingEscalationView.kind`. Added `cast` import to `typing` block at `:64` and `EscalatableRequestKind` to the `from .models import` block. Also tightened `tests/test_projection_helpers.py`'s `_build_controller_for_helpers` return type from `tuple[DelegationController, object]` to `tuple[DelegationController, PendingRequestStore]` (the `object` annotation was erasing the `prs` type, causing Pyright failures on all four `prs.create()` callsites in the new test file).
  - **`b66d838f` (closeout 2):** code-quality review I1 fix. Two `@pytest.mark.skip(reason=...)` decorators in `tests/test_delegation_controller.py` cited fabricated sibling-test names as "partial coverage" for the skipped invariants — `test_start_with_unparseable_request_creates_causal_record_status` (line 1424) and `test_decide_rejects_stale_request_id_after_escalation` without `re-` (line 2380) — neither test exists in the package. Replaced both citations with honest "no current sibling coverage" framing that explains why no real sibling could exist (D4 carve-out and `request_already_decided` invariant both intrinsically require Mode A flows in the pre-Phase-G interregnum, so no non-Mode-A coverage path exists). False confidence about coverage continuity is the failure mode the skip-reason rubric exists to prevent.

  **Convergence-map gap recorded:** L4's "pure helpers, no signaling at callsites" lock specified the new RAISE behavior of `_project_request_to_view` but failed to audit the function's full call graph for transitive effects. The convergence map's W3 originally enumerated 3 breaking tests (the post-decide poll-projection cases); the actual count was 8. Five additional tests broke because `_finalize_turn:1644-1650` calls `_project_request_to_view` directly with the captured request during the synchronous escalation return path. Under the new L4 guard, every unknown-kind capture (e.g., `_permissions_request` producing `kind="unknown"`) now raises during `start()` or `decide()` execution itself, not just at projection. The implementer caught this during execution and (after controller adjudication) skipped all 8 Mode A + Mode B tests with substantive Phase G/Task 17 unblock citations. **Generalizable trap for future shape-rewrite tasks:** when adding runtime guards or behavioral changes to a helper, audit ALL callers in the call graph, not just the explicit signature-change callsite. The "what does this function's full caller surface look like" pass should be a standing item in convergence-map drafting.

  **Skip-set classification (8 tests):**
  - **Mode A — `_finalize_turn` callsite raises on unknown-kind capture (6 tests).** `tests/test_delegation_controller.py:1360, 1418, 1737, 2372` and `tests/test_delegate_start_integration.py:617, 1062`. All cite Phase G (Task 17) as unblock owner — Task 17 will add unknown-kind handling at the L6 callsite so `start()`/`decide()` can route `kind="unknown"` captures appropriately (terminalize the job vs. raise a synthetic null-projection variant per the Unknown-kind contract). Two of the six (`:1418` D4 carve-out; `:2372` request_already_decided invariant) have no current sibling coverage by structural necessity (any path exercising the invariant hits the same Mode A break). Coverage restored when Task 17 lifts the L6 callsite guard.
  - **Mode B — `parked_request_id` structurally unset in current production path (2 tests).** `tests/test_delegation_controller.py:2588` and `tests/test_delegate_start_integration.py:793`. `delegation_job_store.update_parked_request` exists but has zero production callsites; the new job-anchored helper short-circuits at the unparked guard for every legitimate flow. Cite Phase F/G + W1 as unblock — worker-runner wiring will land the `update_parked_request(job_id, request_id)` call at capture time. Helper-shape coverage moved to `tests/test_projection_helpers.py` (8 helper-isolated unit tests).

  **Triage table evolution:** original convergence map listed 3 breaking tests (1 Mode B + 2 originally classified as Mode B but actually Mode A). After implementer execution, controller adjudicated upgrade of all 8 to disposition (b) skip — the original (c) direct-store assertion was unreachable for Mode A because the failing `decide()` raises before the post-decide `prs.get(...)` assertion executes.

  **Lock conformance throughout:** L1 (helper-only scope — only the two helpers + constant + single callsite touched), L2 (reused `UnknownKindInEscalationProjection`, no new exceptions), L3 (`_ESCALATABLE_REQUEST_KINDS` matches `EscalatableRequestKind` Literal exactly), L4 (zero side effects in either helper — no `signal_internal_abort`, no `logger.X`, no journal writes, no store mutations), L5 (`_project_pending_escalation(self, job: DelegationJob)`, single callsite at `:1067` updated), L6 (`_finalize_turn` D4 write at `:1608-1610` and escalation-return at `:1644-1650` unchanged), L7 (no Phase F/G/H work — no `update_parked_request` wiring, no `poll()` catch+signal, no `start()` parked-projection branch, no `contracts.md` edits, no `completion_origin` writes). Spec compliance review confirmed all locks honored; code quality review surfaced 1 Important finding (closeout 2: fabricated sibling citations) and 4 Minor findings (1 deferred per E14.1 above; 3 declined as either local-style-match — no docstrings on helper-isolated tests matches package precedent — or already-addressed in closeout 1).

  **Independent verification:** spec compliance review (sonnet, general-purpose) verified L1-L7 honored end-to-end, all 8 skip decorators substantive, `test_projection_helpers.py` 8 cases covering plan §14.1 invariants. Code quality review (`superpowers:code-reviewer`) approved with 1 Important + 4 Minor; re-review after closeout 2 confirmed I1 closed (fabricated names removed; honest framing in place; no collateral changes).

### From Phase F Task 16 + closeouts

- **[Resolved `667ed20e` + closeout-fix `80a88cab` + closeout-docs (this commit)]** Phase F Task 16 rewrote `_server_request_handler` for the worker-driven async-decide model: 6 sentinel raise sites at terminal worker branches (`internal_abort`, `dispatch_failed`, `timeout_cancel_dispatch_failed`, `timeout_interrupt_failed`, `timeout_interrupt_succeeded`, `unknown_kind_interrupt_transport_failure`), 3 new helpers (`_handle_timeout_wake`, `_write_completion_and_audit_timeout`, `_repo_root_for_journal`), `ResolutionRegistry` initialized in `__init__` for cross-thread coordination, `_APPROVAL_OPERATOR_WINDOW_SECONDS = 900` module constant, `cast(EscalatableRequestKind, parsed.kind)` at the registry-register callsite (`delegation_controller.py:885`) and at the projection construction site (`:1581` — L4 fix), plus `respond(request_id, result)` method added to `AppServerRuntimeSession` for worker-side JSON-RPC response forwarding. New test file `tests/test_handler_branches_integration.py` (14 tests; 11 pass + 3 skip). Suite: 968 passing → 968 passing + 26 newly skipped (Task 17 unblock surface — see F16.2 + W6 amendment story below).

  **A2 closed by L8 raise-site comments** (`667ed20e`). Each of the 6 sentinel raise sites carries a structured comment enumerating cleanup obligations as a YES/n/a list (completed journal write, `update_parked_request(None)`, `registry.discard`, cleanup helper) plus the spec sentinel reason. Closeout-fix `80a88cab` normalized the pre-capture site at line 824 to the same bulleted format as the other 5 sites.

  **A3 closed by L11 audit** (per `65f270ab`, with Task 16 confirmation). Pre-Task-16 commit `65f270ab` already added `cast(EscalatableRequestKind, request.kind)` at the projection construction site; Task 16's L11 audit confirms the live code at `delegation_controller.py:1581` and adds a symmetric `cast` at the `register()` callsite at `:885` (per L4 fix). No additional production change needed for A3 closure beyond the audit annotation.

  **C10.4 closed by L7 worker-completion writes** (`667ed20e`). Task 16 added 4 `completion_origin="worker_completed"` writes at terminal worker branches: 3 inline (`internal_abort`, `dispatch_failed`, decide-success) and 1 in the `_write_completion_and_audit_timeout` helper (called from 4 timeout sub-branches). The `OperationJournalEntry.completion_origin` Literal field now has production callsites; Phase C's deferred wiring is production-active.

  **Three-commit chain (canonical `feat + closeout-fix + closeout-docs` shape):**
  - **`667ed20e` (feat):** primary handler rewrite + 3 helpers + registry init + module constant + L4 cast at both callsites + `respond()` protocol method + new 14-test integration file + 26 skip decorators (21 in `test_delegation_controller.py`, 5 in `test_delegate_start_integration.py`) for the Phase G Task 17 unblock surface.
  - **`80a88cab` (closeout-fix):** 3 review-driven cleanup items. (1) Removed dead `dispatch_result` / `dispatch_error` parameters from `_write_completion_and_audit_timeout` (signature + 4 production callsites + 2 test calls; both spec and code-quality reviewers flagged + Pyright `★` warnings corroborated; `dispatch_error` was also semantically misleading at the interrupt-failure callsite which received an interrupt error, not a dispatch error). (2) Normalized sentinel comment at `delegation_controller.py:824` to the bulleted YES/n/a format matching the other 5 sentinel sites. (3) Extracted `_TASK_17_DEADLOCK_REASON` module-level constant in each test file (per-module, no shared helper) to replace the verbatim mechanism string repeated 21× in `test_delegation_controller.py` and 5× in `test_delegate_start_integration.py`; net delete of ~234 lines, future Task 17 unblock becomes a 1-line edit per file.
  - **`<this commit>` (closeout-docs):** convergence map + dispatch packet + carry-forward closures (this entry); `task-16-convergence-map.md` carries post-implementation W6 amendment at line 89 (8 + 26 = 34 total Task 17 unblock surface) and L10 correction at line 71 (D4 carve-out gap at `_finalize_turn:1645`).

  **W6 amendment (post-implementation discovery):** The convergence map's W6 watchpoint originally identified that 8 Mode A/B tests would deadlock under the new handler. Implementer execution surfaced 26 ADDITIONAL currently-passing tests that drive `controller.start()` or `controller.decide()` through parkable requests and now hang on `registry.wait(...)` until the 900-second timer fires. Total Phase G Task 17 unblock surface: 8 + 26 = 34 tests. After 3-option adjudication (A: skip with Task 17 cite; B: `approval_window_seconds` timeout injection; C: optional `_registry` mock injection), user chose Option A with tightened controls — exact-test census (no estimates), verbatim Phase G Task 17 mechanism citation in every skip reason, no `__init__` signature changes (B and C both rejected as they would either change test semantics or hide the missing public coordination surface behind a test-only oracle). F16.2 records the 26-test list with `spawn_worker` + `wait_for_parked` unblock criterion. **Generalizable trap:** when introducing a new blocking call into a previously-synchronous code path, audit the FULL caller surface for currently-passing tests, not only those already classified as deferred. Convergence-map drafting for blocking-behavior-change tasks must include a "currently-passing-tests-that-touch-this-path" census before locking the `N + M passing` target.

  **L10 correction (post-implementation discovery):** L10 said `_finalize_turn`'s D4 carve-out at `:1645` handles the unknown-kind interrupt-success projection. Implementer found that the live carve-out does NOT cover `interrupted_by_unknown=True + parse_failed` — `_project_request_to_view` raises `UnknownKindInEscalationProjection` regardless. Resolution: the path is skipped at `test_handler_branches_integration.py:539` with same authority as Mode A (Task 17 L6 callsite handling); no Task 16 production change required. Spec reviewer independently verified by tracing control flow at `_finalize_turn:2214`.

  **Lock conformance throughout:** L1 (6 sentinel raise sites in 1:1 correspondence with spec reasons), L2 (each raise is the last step after durable cleanup), L3 (`unknown_kind_interrupt_transport_failure` remains sole pre-capture sentinel reason), L4 (`cast(EscalatableRequestKind, parsed.kind)` at both callsites — fix landed in feat), L5 (parkable-capture 6-step order: `create` → SET `update_parked_request` → persist `needs_escalation` → `register` → `announce_parked` → `wait`), L6 (8 production `update_parked_request` callsites: 1 SET at capture + 7 CLEAR at terminal branches), L7 (4 `completion_origin="worker_completed"` writes — closes C10.4), L8 (raise-site comments enumerate cleanup obligations — closes A2), L9 (decide-success + cancel-success rows finalizer-routed; tests skipped to Phase H Task 19 per F16.1), L10 (TurnTerminalWithoutEscalation row deferred via L10 correction above to Task 17), L11 (closeout-docs audit confirms A3 resolution at `:1581`). Watchpoints W1-W13 all honored — spec compliance review independently verified each lock and watchpoint against the working tree before the feat commit landed.

  **Validation:** 968 passed / 37 skipped / 0 failed (full suite, unchanged across feat → closeout-fix → closeout-docs); pyright on touched files 0 errors / 0 warnings (was 0 + 2 ★ in feat; ★ removed by closeout-fix); W7=6, L6=8, L7=4 invariants satisfied throughout the chain. Pre-existing `runtime.py:270` Pyright error remains (unrelated `TurnStatus` literal narrowing — pre-dates Task 16).

  **New carry-forward items (still Open):** F16.1 (finalizer-routed integration test coverage; Phase H Task 19 unblock — 2 tests, same-commit un-skip with the `_finalize_turn` Captured-Request Terminal Guard) and F16.2 (Phase G Task 17 unblock surface — 26 tests with full `<file>::<test_name>` list in Open items §F16.2; remove decorators when `spawn_worker` + `wait_for_parked` land).

  **Independent verification:** spec compliance review (`general-purpose`, sonnet) verified L1-L11 + W1-W13 + spec sentinel section (6 raise sites map 1:1 to spec reasons with all invariant table rows satisfied) + branch matrix coverage (8 rows + L10 row 9) + `respond()` protocol amendment (matches spec verbatim at `2026-04-23-deferred-approval-response-design.md:1593-1599`); code quality review (`feature-dev:code-reviewer`) approved with 0 BLOCKING + 0 MAJOR + 3 MINOR (all addressed in closeout-fix `80a88cab`). Both reviewers ran sequentially on the working tree before the feat commit landed.

  **Convergence-map-as-binding-authority pattern continues:** the binding `task-16-convergence-map.md` working doc (264 lines: 11 locks + 13 watchpoints + 8-row branch matrix + 9-test triage) was drafted via the user's two-read protocol AND so was the dispatch packet (212 lines: implementer prompt + acceptance criteria + grep invariants). Pattern locked for Task-16-class dispatches: two-read applies to BOTH convergence map AND dispatch packet. Working artifacts stay untracked through implementer execution + reviews, then land in the closeout-docs commit alongside the carry-forward closures.

### From Phase G Task 17 + closeouts

- **[Resolved `8dd15971` + closeout-fix `dc90c1d9` + closeout-docs (this commit)]** Phase G Task 17 rewrote `DelegationController.start()` for the capture-ready handshake under the async-decide model. The synchronous `_execute_live_turn(...)` call inside `start()` (live anchor `delegation_controller.py:737`) was replaced with `spawn_worker(...)` (worker thread spawn from Task 15 scaffold) + `self._registry.wait_for_parked(job_id, timeout_seconds=START_OUTCOME_WAIT_SECONDS)` (main-thread blocker from Task 12) + dispatch through a new `_dispatch_parked_capture_outcome(...)` helper. The helper uses an exhaustive `match` over all 5 `ParkedCaptureResult` variants (`Parked` / `TurnCompletedWithoutCapture` / `TurnTerminalWithoutEscalation` / `WorkerFailed` / `StartWaitElapsed`) with `case _: assert_never(outcome)` as the final arm per W11; signal-then-raise ordering for `Parked`-with-bad-projection invariant violations per L8 (worker is parked in `registry.wait()`, so `signal_internal_abort` MUST fire before the main-thread raise to prevent the worker leaking); reason-preservation for `WorkerFailed` per L7 (worker-raised `DelegationStartError` reasons flow through intact; `worker_failed_before_capture` is the fallback). New module-level constant `START_OUTCOME_WAIT_SECONDS: float = 30` near `_APPROVAL_OPERATOR_WINDOW_SECONDS`. New imports: 6 names from `.resolution_registry` (`Parked`, `ParkedCaptureResult`, `StartWaitElapsed`, `TurnCompletedWithoutCapture`, `TurnTerminalWithoutEscalation`, `WorkerFailed`) extending the existing import line, plus `from .worker_runner import spawn_worker` and `assert_never` added to the `from typing import ...` line. New test file `tests/test_delegate_start_async_integration.py` (8 acceptance tests covering all 5 `ParkedCaptureResult` variants + 2 `Parked`-projection invariant-violation sub-cases + 1 reason-preservation case, all using module-local `_build_controller` per W4). 23 Bucket A skip decorators removed (post-L13 effective; originally 24 planned at dispatch); `_TASK_17_DEADLOCK_REASON` constant renamed to `_TASK_18_DECIDE_SIGNAL_REASON` in both test files with reason text rewritten per L6 to cite Task 18's `reserve()` + `commit_signal()` mechanism + audit-discipline framing; 12 Bucket B decorators retained (post-L13; 9 constant-backed + 3 callsite-specific Mode A explicitly rewritten to use the renamed constant). Suite: 968 → 999 passing (= 968 + 8 new + 23 unskipped post-L13); 14 skipped (12 Bucket B citing Task 18 + 2 F16.1 citing Task 19); 0 failed. Wall-clock 19.65s feat / 18.80s closeout-fix.

  **L11 carve-out at `_finalize_turn`** (`667ed20e`-derived live anchor `:2358` pre-edit, `:2367` post-edit): the existing `if captured_request.kind in _CANCEL_CAPABLE_KINDS or interrupted_by_unknown:` entry was split into `if interrupted_by_unknown: final_status = "unknown"` / `elif captured_request.kind in _CANCEL_CAPABLE_KINDS: final_status = "needs_escalation"` so that unknown-kind captures fall through to the existing non-escalation return at `:1512-1517` (preserving `_emit_terminal_outcome_if_needed` + runtime release + session close) instead of being routed into `_project_request_to_view`, which raises `UnknownKindInEscalationProjection` for `kind="unknown"`. Authority: spec §Unknown-kind contract at `design.md:1725-1727`. The `_CANCEL_CAPABLE_KINDS`-branch body (terminal-status mapping, one-snapshot rule, Request-to-job mapping table, D4 blind-write suppression) is UNCHANGED — that's W2-narrowed Task 19 territory. Carve-out unblocks 4 tests in Bucket A: `test_unknown_kind_parse_failure_terminalizes_unknown` (L10 barrier at `test_handler_branches_integration.py:539`) plus the 3 Mode A unknown-kind tests at `test_delegation_controller.py:1377`, `:1436`, and `test_delegate_start_integration.py:628`.

  **F16.2 split (post-L13).** Of the original 26 F16.2 tests: 17 closed by Task 17 in Bucket A (10 start-flow + 7 decide-rejects, after L13 reclassified `:1522` to Bucket B); remaining 9 (8 originally classified Bucket B + 1 reclassified per L13: `test_start_post_turn_finalization_failure_marks_job_unknown_and_cleans_up`) move to new G17.1 carry-forward for Phase G Task 18 closure. F16.2 row in Open items annotated as "split per Task 17"; G17.1 added as new Open item (see below).

  **Mode A partial closure (Task 17) — superseded by full closure in Task 18.** 6 tests total from Phase E Task 14: 3 close in Bucket A under Task 17 (`test_delegation_controller.py:1377` `test_start_with_unknown_request_interrupts_and_escalates`; `:1436` `test_start_with_unparseable_request_creates_minimal_causal_record`; `test_delegate_start_integration.py:628` `test_e2e_unknown_request_kind_interrupts_and_escalates`); 3 deferred to Task 18 (`test_delegation_controller.py:1761` `test_decide_approve_can_reescalate_with_new_pending_request`; `:2406` `test_decide_rejects_stale_request_id_after_reescalation`; `test_delegate_start_integration.py:1077` `test_decide_reescalation_uses_pending_escalation_key`). The 3 deferred tests had hand-written callsite-specific reason strings before Task 17 — they were explicitly rewritten in the feat commit to use the renamed `_TASK_18_DECIDE_SIGNAL_REASON` constant per L6. **Task 18 final disposition: all 3 deferred tests close in Task 18 (3/3); Mode A row fully retires (6 of 6). Round-7 confirmed via production-path coverage post-inline fix at `delegation_controller.py:1025-1037` (re-park gate lift); test-side PSR.create pre-seed workarounds removed at 3 sites (`test_delegation_controller.py:1820-1836, 2460-2472`; `test_delegate_start_integration.py:1127-1138`). See "From Phase G Task 18 + closeouts" closeout entry below for full narrative.**

  **Mode B fully closed.** 2 tests total from Phase E Task 14: both close in Bucket A (`test_delegation_controller.py:2626` `test_poll_needs_escalation_projects_pending_request_without_raw_ids`; `test_delegate_start_integration.py:805` `test_delegate_poll_needs_escalation_returns_projected_request`). Worker-runner wiring (Task 16's `update_parked_request(job_id, request_id)` SET at capture) is now production-active, so `_project_pending_escalation` no longer short-circuits at the unparked guard for legitimate flows.

  **L10 barrier closed.** Single decorator at `test_handler_branches_integration.py:539` (`test_unknown_kind_parse_failure_terminalizes_unknown`) removed in Bucket A; carve-out at `_finalize_turn` (L11) enables the unknown-kind path. Following Task 16 bookkeeping convention, L10 was tracked inside the Task 16 closeout narrative (not a top-level Open row); now closed here in Task 17 closeout-docs.

  **Three-commit chain (canonical `feat + closeout-fix + closeout-docs` shape):**
  - **`8dd15971` (feat):** primary `start()` rewrite + `_dispatch_parked_capture_outcome` helper + L11 carve-out at `_finalize_turn` + new test file `tests/test_delegate_start_async_integration.py` (8 acceptance tests) + 23 Bucket A decorator removals + constant rename `_TASK_17_DEADLOCK_REASON` → `_TASK_18_DECIDE_SIGNAL_REASON` (both files) + reason text rewrite per L6 + 3 explicit callsite-specific Mode A Bucket B reason-string rewrites + 8 Bucket A test-body assertion updates per L12 (deferred-escalation semantics: `agent_context=None` on `Parked`, request status `"pending"` until `decide()` resumes, escalation audits deferred until `_finalize_turn` runs on the resumed worker) + 1 Bucket A→B reclassification per L13 (`test_start_post_turn_finalization_failure_marks_job_unknown_and_cleans_up`).
  - **`dc90c1d9` (closeout-fix):** 3 P3 nits + 1 forwarded docstring extension + optional cleanup. F1: import order swap so `.runtime` precedes `.worker_runner` (alphabetical local-import ordering at `delegation_controller.py:113-114`). F2: test section numbering reorder so variants enumerate `1 → 2 → 3 → 4 → 4 → 5` (matches branch-matrix order in convergence map; was `1 → 2 → 3 → 5 → 4 → 4`). F3: tightened `agent_context` assertion in `test_e2e_command_approval_produces_escalation` from "may be None but key must be present" (stale framing) to `assert payload["agent_context"] is None` (deferred-escalation invariant per L12). Note 1: appended one sentence to L13 reclassified test docstring noting that the body assertions use the `worker_failed_before_capture` fallback shape (not the expected Task-18 failure path) and must be rewritten when Task 18 unblocks the test. Optional cleanup: removed unused `repo_root = tmp_path / "repo"` + `repo_root.mkdir()` from all 5 dispatch-direct tests in `tests/test_delegate_start_async_integration.py` (the 3 tests going through `controller.start(...)` keep `repo_root`). No behavioral changes; suite count preserved exactly (999/14 → 999/14).
  - **`<this commit>` (closeout-docs):** convergence map + dispatch packet + carry-forward closures (this entry); `task-17-convergence-map.md` carries post-feat L11/L12/L13 amendments + W2 narrowing + full Post-implementer adjudication record at end of doc.

  **BLOCKED-1 adjudication story (L11).** First-iteration implementer reported BLOCKED on the W2 boundary: the Bucket A tests for unknown-kind paths (`test_unknown_kind_parse_failure_terminalizes_unknown` L10 barrier + 3 Mode A tests) require `_finalize_turn`'s L6 callsite to NOT call `_project_request_to_view` for `kind="unknown"` (which raises `UnknownKindInEscalationProjection`), but the originally-stated W2 said "Do NOT modify `_finalize_turn` body." The controller adjudicated: spec §Unknown-kind contract at `design.md:1725-1727` explicitly attributes the entry-condition split to Packet 1 line edits; spec §`_finalize_turn` Captured-Request Terminal Guard at `design.md:1738-1808` is a SEPARATE Task 19 concern (cancel/timeout terminal mapping). W2 was over-broad — it cited "Captured-Request Terminal Guard" in the rationale but worded the rule as blanket non-modification, conflating two logically distinct spec sections. W2 was narrowed to scope only the Captured-Request Terminal Guard logic; L11 was added to the convergence map with the required code shape (entry-condition split + `final_status="unknown"` + fall-through to existing non-escalation return). Implementer applied the carve-out per the exact L11 shape (no `return updated_job` short-circuit that would skip post-turn cleanup); 4 affected Bucket A tests passed on re-run. Full provenance at convergence map's L11 entry + Post-implementer adjudication record.

  **Post-feat adjudication story (L12 + L13).** Three implementer post-feat decisions surfaced during code-quality review and were retroactively adjudicated:
  - **L12 (assertion-shape updates).** Removing skip decorators on tests originally written under the synchronous model meant their body assertions (`agent_context is not None` on `Parked`-path escalations, request status `"resolved"` after `start()` returns, escalation-audit events fired during `start()`, `result.status == "completed"` for `request_user_input` paths that now park) would fail under the new async model. The implementer updated assertion shapes inline as part of the decorator removals. Spec authority: §Capture-ready handshake at `design.md:785-797` (deferred-escalation: `agent_context=None` mandate; request status `pending` until `decide()` resumes; escalation audits deferred). Treated as a convergence-map gap (L10 specified the decorator-removal mechanism but didn't anticipate the assertion-update obligation), not an implementer process violation. L12 records the obligation prospectively for future tasks.
  - **L13 (Bucket A→B reclassification).** `test_start_post_turn_finalization_failure_marks_job_unknown_and_cleans_up` at `test_delegation_controller.py:1522` was reclassified from Bucket A to Bucket B during the post-feat audit. The test sabotages the SECOND `journal.append_audit_event` call (escalation audit emit inside `_finalize_turn`); under the synchronous model that second call was reachable from `start()`, but under the new async model `_finalize_turn` runs on the worker thread AFTER `decide()` calls `commit_signal()` to resume — so the sabotaged failure path is unreachable from `start()` alone. Substantive justification decisive; reclassification stands. **Process precedent:** this reclassification SHOULD have been BLOCKED + asked rather than executed unilaterally. Future implementers must report BLOCKED for ANY bucket reclassification — even when the substantive reasoning is decisive — so that the controller updates the convergence map's binding counts BEFORE the feat commit lands. The L13 record exists to make the exception visible without normalizing the path.

  **Lock conformance throughout (L1-L13 + W1-W13):** L1 = post-L13 23+12 split honored; L2 = synchronous `_execute_live_turn(...)` call replaced; L3 = `START_OUTCOME_WAIT_SECONDS = 30` module constant added; L4 = exhaustive `match` over all 5 `ParkedCaptureResult` variants with `case _: assert_never(outcome)` final arm; L5 = 6 imports from `.resolution_registry` + `from .worker_runner import spawn_worker` + `assert_never` added; L6 = constant rename + reason text rewrite per spec; L7 = `WorkerFailed` arm preserves `DelegationStartError.reason` from worker; L8 = signal-then-raise ordering for both `Parked`-projection invariant-violation sub-cases (null AND raise); L9 = new file `tests/test_delegate_start_async_integration.py` distinct from `tests/test_delegate_start_integration.py`; L10 = post-L13 23 effective Bucket A removals (24 originally planned − 1 reclassified per L13); L11 = D4 carve-out shape per spec §Unknown-kind contract `design.md:1725-1727`; L12 = 5 assertion-shape categories (deferred `agent_context`, deferred D4 `resolved`, deferred escalation audits, parked-instead-of-completed for `request_user_input`, terminal-job for unknown-kind) verified inline; L13 = reclassified test docstring extended in fix commit `dc90c1d9` per code-quality reviewer Note 1. W1 = no `decide()` body edits; W2 = narrowed to terminal-guard logic only (cancel/timeout mapping; D4 blind-write suppression; one-snapshot rule); W3 = 6 sentinel raise sites preserved (grep returns 6 throughout the chain); W4 = module-local `_build_controller` + built-in pytest fixtures + `unittest.mock.MagicMock`; W5 = full-suite 19.65s feat / 18.80s fix wall-clock + order-independence smoke 106/12 identical both directions; W6 = no re-imports of `cast`, `EscalatableRequestKind`, `ResolutionRegistry`, `DecisionResolution`, `InternalAbort`; W7 = live anchors used (plan-cited line numbers stale); W8 = F16.1 decorators at `test_handler_branches_integration.py:161, 179` UNTOUCHED; W9 = Task 16's `__init__` registry init reused, not shadowed; W10 = no `runtime.py` touches; W11 = `assert_never` final arm verified; W12 = no `pass`-bodied tests committed; W13 = legacy `DelegationEscalation` construction site (live anchor `:2240` pre-feat, `:2389` post-feat) untouched.

  **Validation:** 999 passed / 14 skipped / 0 failed (full suite, unchanged across feat → closeout-fix → closeout-docs); ruff check shows 3 pre-existing F401 errors in untouched test files (`test_delegation_exceptions.py:5`, `test_delegation_sanitization.py:5`, `test_pending_request_store_mutators.py:5` — all pre-existing per stash test against baseline `f3cfa61a`); W3=6 invariant satisfied throughout the chain; pre-existing `runtime.py:270` Pyright `TurnStatus` literal narrowing error remains (RT.1 carry-forward — see below).

  **New carry-forward items (still Open):** G17.1 (Phase G Task 18 unblock surface — 9 F16.2 Bucket B tests, same-commit un-skip with `reserve()` + `commit_signal()` rewrite; Task 18 dispatch will close all 12 Bucket B = 9 G17.1 + 3 Mode A defer). RT.1 (`runtime.py:270` Pyright `TurnStatus` literal narrowing pre-existing per Task 16 G34 + Task 17 W10; closes at end-of-Phase-G or end-of-Packet-1 typing polish). **Task 18 final disposition (post-Task-17): G17.1 closed by Task 18 (3 delete + 6 reclassify-to-G18.1); G18.1 newly introduced as Phase H Task 19 unblock surface; TT.1 newly promoted alongside RT.1 for end-of-Phase-G typing polish. See "From Phase G Task 18 + closeouts" closeout entry below.**

  **Independent verification:** spec compliance review (`general-purpose`, sonnet, agent name `task-17-spec-reviewer`) verified L1-L13 + W1-W13 with file/line citations against the working tree; code quality review (`general-purpose`, sonnet, agent name `task-17-code-quality-reviewer`) approved with 0 P1 + 0 P2 + 3 P3 findings (all addressed in closeout-fix `dc90c1d9`) + 2 forwarded notes triaged (Note 1 → docstring extension applied; Note 2 → leave as-is, W5 covers concurrency).

  **Convergence-map-as-binding-authority pattern continues:** the binding `task-17-convergence-map.md` working doc (399 lines: 13 locks post-amendment + 13 watchpoints + 5-row branch matrix + per-test triage + Post-implementer adjudication record) was drafted via the user's two-read protocol AND so was the dispatch packet (364 lines: implementer prompt + acceptance criteria + grep invariants + reporting contract). The convergence map was AMENDED post-feat with L11 (BLOCKED-1 resolution), L12 (assertion-update obligation), L13 (Bucket A→B reclassification with process precedent), and the W2 narrowing — preserving the original dispatch-time framing alongside the post-feat reality so the historical provenance is auditable. Working artifacts stayed untracked through implementer execution + reviews + post-feat amendment cycle, then land in this closeout-docs commit alongside the carry-forward closures. Post-implementer adjudication record at end of convergence map captures L11/L12/L13 provenance.

### From Phase G Task 18 + closeouts

- **[Resolved `2606fb03` + closeout-fix `b8e7f9ce` + closeout-docs (this commit)]** Phase G Task 18 rewrote `DelegationController.decide()` for the reservation two-phase protocol under the async-decide model. The synchronous post-validation block at `delegation_controller.py:2560-2675` (pre-feat live anchors) was replaced by: validate (unchanged) → `_build_response_payload(decision, answers, request)` (NEW helper, L4) → `DecisionResolution(payload, kind=request.kind, action=decision)` → `reserve(rid, resolution)` → narrow-try journal-intent (with `abort_reservation` rollback per L7b) → `commit_signal(token)` BARE per L14 (no try/except) → audit (post-commit, non-gating, action=decision per L7a) → return `DelegationDecisionResult(decision_accepted=True, ...)`. New helper `_build_response_payload(self, *, decision, answers, request) -> dict[str, Any]` at `:2634-2679` implements the 6-row binding contract from spec §Response payload mapping (`design.md:1667-1672`): `{"decision": "accept"}` for approve on command/file kinds; `{"answers": <validated wrapped dict>}` for approve on RUI; `{"decision": "decline"}` for deny on command/file (NOT `"reject"` per spec §1670 + §1682-1687); `{"answers": {}}` empty-fallback for deny on RUI (per spec §1689-1697); defensive `RuntimeError` for unexpected combinations. **L6 retired:** `_decided_request_ids` set fully removed across 4 callsites (the reservation CAS is the single authority for "already decided"; grep returns 0 post-Task-18). New test file `tests/test_delegate_decide_async_integration.py` (15 test names; 18 sub-cases under parametrization). Suite: 999 → 1020 passing (= 999 + 18 new − 3 deleted CDFE tests + 6 reclassified to G18.1; net +21 vs feat baseline, then Round-7 adds 3 wire-shape regression sub-cases for +21 total). 8 skipped (6 G18.1 + 2 F16.1); 0 failed. Wall-clock 124s feat / 124s closeout-fix.

  **Bucket B disposition (12 of 14 retentions, post-Task-18 final):**
  - **3 close** (Mode A defer mechanism-only — re-escalation continues turn, finalizer doesn't run on original capture): `tests/test_delegation_controller.py:1781::test_decide_approve_can_reescalate_with_new_pending_request`, `:2410::test_decide_rejects_stale_request_id_after_reescalation`, `tests/test_delegate_start_integration.py:1063::test_decide_reescalation_uses_pending_escalation_key`. All 3 exercise the production re-park code path post-Round-7 inline fix (`delegation_controller.py:1025-1037` re-park gate lift); test-side PSR.create pre-seed workarounds removed.
  - **3 delete** (obsolete CDFE per L11 — code path no longer exists under reserve+commit_signal): `tests/test_delegation_controller.py::test_decide_approve_turn_failure_raises_committed_decision_finalization_error`, `::test_decide_approve_post_turn_journal_failure_raises_committed_decision_finalization_error`, `::test_decide_deny_post_commit_failure_raises_committed_decision_finalization_error`. CDFE class itself preserved (out of scope for L11).
  - **6 reclassify to G18.1** (per L12 — finalizer-dependent; Phase H Task 19 owner): full G18.1 list above in Open items.

  **Mode A row CLOSED (full retirement, 6 of 6).** 3 closed by Task 17 Bucket A (unknown-kind paths) + 3 closed by Task 18 (re-escalation paths). Round-7 confirmed via production-path coverage of all 3 Task-18-closed tests. **Round-6 framing superseded by Round-7:** the Round-6 addendum classified the worker stale-wrapper-keys + re-park PSR.create gate as out-of-Task-18 carry-forward observations (and the 3 Mode A defer tests were closed via test-side workaround). Round-7 superseded that disposition by fixing both gaps inline within Task 18 via narrow W16 adjudication; the test-side workarounds were removed at 3 sites. Mode A row now fully retires via the production re-park path, not via test-side simulation pre-seeds. Convergence map preserves Round-6 verbatim with explicit supersession markers at the start of Round-7 — the audit trail (Round-6 framing → Round-7 reclassification + fix → final disposition) is preserved as the project record.

  **F16.2 lineage annotation.** F16.2 stays Open via G18.1 lineage; closes when Task 19 lands the Captured-Request Terminal Guard rewrite. The 3 L11 deletions (obsolete CDFE) are noted as `[deleted by Task 18 per L11]` markers in F16.2's full test list above; the 6 L12 reclassifications are tracked in the new G18.1 entry.

  **Three-commit chain (canonical `feat + closeout-fix + closeout-docs` shape):**
  - **`2606fb03` (feat):** primary `decide()` rewrite + `_build_response_payload` helper + `DecisionResolution` extension at producer side + new test file `tests/test_delegate_decide_async_integration.py` (initial 15-test set; 18 sub-cases under parametrization) + L11 deletions of 3 CDFE tests + constant rename `_TASK_18_DECIDE_SIGNAL_REASON` → `_TASK_19_FINALIZER_GUARD_REASON` (both files) + reason text rewrite per L10 (Task 19 mechanism citation) + 6 G18.1 decorators retained with renamed constant + 3 Mode A defer Bucket B unskips with narrow test-mechanics patches per Round-6 adjudication D. **L4 Path A verification artifact** filed in commit message body: pinned-version Codex App Server v0.117.0 JSON Schema fixture at `tests/fixtures/codex-app-server/0.117.0/ToolRequestUserInputResponse.json` (defines wire shape `{"answers": {<qid>: {"answers": [<string>...]}}}`); ground-truth verification in `test_build_response_payload_per_kind_decision[request_user_input-approve-...]` at `tests/test_delegate_decide_async_integration.py:612-669`. Path A option 3 (pinned-version integration test). **L7a incidental fix:** audit `action=decision` fixes pre-existing hardcoded `action='approve'` bug at the relocated audit site — the old hardcoded value fired for both approve AND deny because it sat BEFORE the deny/approve split at `:2606`. Documented as incidental to the required relocation per spec §1654.
  - **`b8e7f9ce` (closeout-fix):** Path 4 contract-focused fix per code-quality review. (1) **Worker payload dispatch contract correction** — `decide()` produces `DecisionResolution(payload=<bare App Server payload>, kind=..., action=decision)` per L4; the worker resume path at `:1160-1161` was reading the payload via wrapper keys `.get("resolution_action", "approve")` and `.get("response_payload", {})` that don't exist in the new shape, dispatching `respond(rid, {})` to the App Server with default-empty payload. Fix: `DecisionResolution` gains optional `action: Literal["approve","deny"] | None = None` (resolution_registry.py:33-65); `decide()` passes `action=decision` explicitly (operator-action carry, not payload-shape inference — inference is unsafe because approve × RUI with empty `answers` and deny × RUI both yield `{"answers": {}}`); worker resume reads `resolution.payload` directly + `resolution.action` (assertion-checked non-None for the operator-decide branch). Added `Literal` to `typing` imports. (2) **Re-park PSR.create gating fix** — lifted the `if captured_request is None` gate at `:1025-1037`; `_pending_request_store.create(parsed)` runs unconditionally for every parkable server-request; `captured_request` always tracks the most-recent capture for `_finalize_turn`'s terminal-guard mapping. Removed test-side PSR.create pre-seed workarounds at 3 sites (`tests/test_delegation_controller.py:1820-1836, 2460-2472`; `tests/test_delegate_start_integration.py:1127-1138`). (3) **Regression coverage** — tightened test #7 (`test_decide_audit_event_post_commit_non_gating`) to assert `dispatched_payload == {"decision": "accept"}` end-to-end (was: rid-only assertion); added new parameterized `test_decide_worker_dispatches_l4_payload_end_to_end` covering 3 wire shapes (approve-accept, deny-decline, approve-RUI-with-answers) end-to-end via real `decide() → DecisionResolution → commit_signal → worker resume → session.respond` capture. (4) **`tests/test_handler_branches_integration.py:447`** updated to the new `DecisionResolution` shape (`payload={"decision": "accept"}`, `action="approve"` separate). (5) **CQ Minor #3** applied: substring-match the exact decide-side warning via `rec.getMessage()` rather than loose `rec.message.lower()` on `"audit"`, preserving %-arg formatting and avoiding unrelated-log false positives.
  - **`<this commit>` (closeout-docs):** convergence map + dispatch packet + carry-forward closures (this entry); `task-18-convergence-map.md` carries Round-7 addendum with explicit supersession markers preserving Round-6's "out-of-Task-18 carry-forward observations" framing as historical record while documenting Round-7's inline-fix disposition.

  **W16 narrow adjudication (Round-7).** Authorized narrow fixes inside `_execute_live_turn` body for both correctness gaps. W16's original protection was the DEF signature (callers like `spawn_worker(...)` invoke it as `_execute_live_turn(...)`) plus structural integrity of the parkable-capture and resume sub-branches. The Path 4 fixes preserve the def signature and the branch structure; they correct only the payload-shape reads (3 lines) at `:1160-1172` and lift one defective `if captured_request is None` gate (3 lines) at `:1025-1037`. W16 stays in force for all other parts of the body. **Generalizable trap (process precedent candidate):** when `decide()` and the worker resume path hand off via a shared dataclass like `DecisionResolution`, BOTH the producer side AND the consumer side of the contract must be regression-pinned. spec-compliance review reading the producer (helper output matches the table) is necessary but not sufficient; consumer-side end-to-end tests on the wire shape catch the form of bug Round-7 surfaced.

  **Branch-matrix-with-test-coverage note.** The convergence map's branch-matrix table (10 rows: 6 valid kind/decision combinations + 2 timeout sub-rows + 2 invariant sub-rows) maps 1:1 to acceptance test coverage in `tests/test_delegate_decide_async_integration.py`: rows 1-3 (approve/deny/RUI happy paths) covered by `test_decide_returns_3_field_result_on_success_*` + `test_build_response_payload_per_kind_decision`; rows 4-6 (CAS-stale, journal-intent failure, audit non-gating) covered by `test_decide_twice_*` + `test_decide_competing_reservation_*` + `test_decide_aborts_reservation_on_journal_intent_failure` + `test_decide_audit_event_post_commit_non_gating`; rows 7-8 (wire-shape contract end-to-end) covered by Round-7's new `test_decide_worker_dispatches_l4_payload_end_to_end[3 sub-cases]`; rows 9-10 (audit action correctness) covered by `test_decide_audit_action_matches_decision_for_approve` + `_for_deny`. Direct test coverage achieved end-to-end.

  **Hang-verification result (W5).** Full codex-collaboration suite: 1020 passed / 8 skipped (6 G18.1 + 2 F16.1) / 0 failed in 123.70s. W5 order-independence smoke (3-file scope: `test_delegation_controller.py`, `test_delegate_start_integration.py`, `test_delegate_decide_async_integration.py`): forward order 124p/6s in 105.68s; reverse order 124p/6s in 105.51s. Both orderings pass identical counts.

  **G18.1 record explicit.** 6 finalizer-dependent tests deferred to Phase H Task 19 with pre-authorized scope: renames at `:1881, :1927`, body rewrite spec for `:1525` (5-step protocol), L12 assertion-review notes for the remaining 3, constant `_TASK_19_FINALIZER_GUARD_REASON` deletion authorization in same commit. See G18.1 entry in Open items above for full itemization.

  **L4 Path A artifact disposition.** Pinned-version Codex App Server v0.117.0 JSON Schema fixture at `packages/plugins/codex-collaboration/tests/fixtures/codex-app-server/0.117.0/ToolRequestUserInputResponse.json` defines the `request_user_input` approve answers wire shape as `{"answers": {<qid>: {"answers": [<string>...]}}}`. Ground-truth verification in `test_build_response_payload_per_kind_decision[request_user_input-approve-answers2-expected_payload2]` (asserts input `{"q1": ("yes",)}` → output `{"answers": {"q1": {"answers": ["yes"]}}}`). Path A option 3 (pinned-version integration test); does NOT require live App Server fixture/probe or App Server source reading.

  **L7a incidental audit fix.** Pre-existing hardcoded `action='approve'` bug at the prior callsite (sat BEFORE the deny/approve split at `:2606`) fired audit events with `action='approve'` for BOTH approve AND deny decisions. The required relocation per spec §1654 forced rewriting the audit emit; the new site uses `action=decision` (the caller's `"approve"` or `"deny"` argument). Documented as incidental to the required Task-18 relocation, not a separate fix scope.

  **Round-7 supersession of Round-6 dispositions (per user precision requirement).** Round-6 framed the worker stale-wrapper-keys + re-park gate as out-of-Task-18 carry-forward observations (deferred to Phase H disposition). Round-7 superseded that with inline fixes within Task 18 via narrow W16 adjudication. Both gaps are now fixed; Mode A row fully retires (the alternative — partial retirement implied by Round-6's "out of scope" framing — is NOT the final disposition). The convergence map's Round-6 addendum is preserved verbatim with explicit supersession markers at the start of Round-7; Round-6 is the historical record of how the open question was framed at that point in review, NOT the final disposition. Read Round-7 below for what actually shipped.

  **Verification evidence (Phase G CLOSES post-this-commit):**
  - Suite: 1020 passed, 8 skipped (6 G18.1 + 2 F16.1), 0 failed in 123.70s.
  - W5 hang verification: 3-file order-independence smoke passes both orderings 124p/6s in ~105s each.
  - Grep audits (production code): W3=6 (`_WorkerTerminalBranchSignal(reason=` raise sites unchanged from Task 17), W17=2 (`DelegationEscalation(` construction sites preserved at `:833` W17-protected + `:2416` W2-protected), L6=0 (`_decided_request_ids` retired).
  - Grep audits (test code): old constant `_TASK_18_DECIDE_SIGNAL_REASON` count = 0; new constant `_TASK_19_FINALIZER_GUARD_REASON` total = 8 (6 decorators + 2 declarations); new constant decorators = 6.
  - **NEW permanent invariant (Round-7):** old wrapper-key reads = 0 (`resolution.payload.get("resolution_action"...)` and `.get("response_payload"...)` both absent from production code).
  - F16.1 decorators in `test_handler_branches_integration.py` preserved at 2 (W8 invariant from Task 17).
  - Pre-existing `runtime.py:270` Pyright `TurnStatus` literal narrowing remains (RT.1; carried forward).
  - New `_FakeControlPlane` Pyright issues at multiple `test_delegation_controller.py` instantiation sites (TT.1; newly promoted).

  **Lock conformance throughout (L1-L14 + W1-W18):** L1 = post-fix Bucket B 12 = 3 close + 3 delete + 6 reclassify; L2 = synchronous post-validation block at `:2560-2675` replaced by reserve+journal+commit_signal flow; L3 = no `_decided_request_ids` references in new code; L4 = `_build_response_payload` 6-row table per spec §1667-1672 (`decline` not `reject`; empty-fallback for deny-RUI; Path A verification artifact filed); L5 = no MCP boundary edits beyond test fixture shape update at `test_handler_branches_integration.py:447`; L6 = `_decided_request_ids` retired; L7 = post-commit non-gating audit (L7a action=decision; L7b abort-on-intent-failure rollback); L8 = no `signal_internal_abort` calls inside `decide()` body (W14); L9 = new test file distinct from `tests/test_delegate_start_integration.py`; L10 = constant rename and reason text rewrite per spec; L11 = 3 obsolete CDFE tests deleted (CDFE class preserved); L12 = 6 finalizer-dependent tests reclassified to G18.1; L13 = no Bucket A→B reclassifications beyond Round-6 adjudication D's narrow test-mechanics patches; L14 = `commit_signal` BARE (no try/except wrapping). W1 = no `start()` body edits; W2 = `_finalize_turn` body untouched (`:2400` `DelegationEscalation` site preserved post-fix at `:2416` post-line-shift; W17 invariant hold); W3 = 6 sentinel raise sites preserved; W4 = module-local `_build_controller` + built-in pytest fixtures + `unittest.mock`; W5 = full-suite 124s wall-clock + order-independence smoke 124p/6s identical both directions; W6 = no re-imports; W7 = N/A (no analogue from prior tasks); W8 = F16.1 decorators at `test_handler_branches_integration.py:161, 179` UNTOUCHED (still 2); W9 = `__init__` registry init reused; W10 = no `runtime.py` touches; W11 = exhaustive variant matching unchanged from Task 17; W12 = no `pass`-bodied tests committed; W13 = `DelegationEscalation` construction sites preserved; W14 = no `signal_internal_abort` in `decide()`; W15 = no `contracts.md` edits; W16 = NARROW ADJUDICATION applied (Round-7) for worker payload dispatch fix + re-park gate lift; def signature unchanged; structural integrity preserved; W17 = both `DelegationEscalation(` sites preserved post-Task-18; W18 = `_FakeSession` and `_ConfigurableStubSession` class definitions NOT modified globally (Round-6 adjudication D narrow per-test patches only).

  **Independent verification:** spec compliance review and code-quality review both ran sequentially on the working tree; code-quality review surfaced the contract-correctness gap (Round-7 Path 4) which user adjudicated and was fixed in the closeout-fix commit. Final landed code passes all invariant audits.

  **Convergence-map-as-binding-authority pattern continues:** the binding `task-18-convergence-map.md` working doc (now ~657 lines including Round-7 addendum + supersession markers + Verification artifacts section) was drafted via the user's two-read protocol AND so was the dispatch packet (468 lines: implementer prompt + acceptance criteria + grep invariants + reporting contract). The convergence map was AMENDED post-feat with Round-6 (Bucket B mechanics adjudication D) and Round-7 (Path 4 contract-correctness fix) — preserving the original dispatch-time framing alongside successive post-feat realities so the historical provenance is auditable. **Phase G CLOSES with this closeout-docs commit.** Phase H entering carry-forward set: F16.1 (2 unchanged) + RT.1 (unchanged) + TT.1 (newly promoted) + G18.1 (newly introduced; 6 finalizer-dependent tests). F16.2 (1 retention; lineage marker) closes when G18.1 lands.

### From Phase H Task 19 + closeouts

- **[Resolved `1f97b333` + closeout-fix `4409b23c` + closeout-docs (this commit)]** Phase H Task 19 rewrote the captured-request branch of `_finalize_turn` for the one-snapshot terminal guard per spec §`_finalize_turn` Captured-Request Terminal Guard (`design.md:1738-1827`). Replaced legacy kind-based derivation at `:2385-2390` and unconditional D4 write at `:2369-2371` with: snapshot read (L1) → D4 conditional write + three-way warning discipline (L2) → terminal-status mapping (L3) → kind-based fall-through preserving L11 unknown-kind carve-out (L4). L3 implements the binding 4-row table: `resolved+completed→completed`, `resolved+interrupted/failed→unknown`, `canceled+any→canceled`, `pending/None→fall-through`. D4 write now conditional on snapshot status: suppressed for terminal snapshots (`resolved`/`canceled`), suppressed for tombstone (`None`), preserved with anomalous-pending warning for `pending` + `parse_failed=False`, silent for `parse_failed=True`. Non-escalation tail (`:2424-2429`) and no-capture branch (`:2431-2445`) byte-identical to pre-rewrite (W3/W4/L5/L6). `DelegationEscalation(` count = 2 (W7).

  **Same-commit deliverables (feat `1f97b333`):** 6 G18.1 skip decorators removed with full body rewrites (`:1525` canonical lineage-store failure injection; `:1737` 5-step approve protocol; `:1881` deny renamed to `test_decide_deny_marks_job_completed_and_closes_runtime` with `job.status=="completed"` per spec deny→decline semantics; `:1927` deny terminal outcome with `terminal_status=="completed"`; `:857, :934` MCP E2E with bounded-poll). 2 F16.1 skip decorators removed with concrete bodies (`test_happy_path_decide_approve_success` resolved+completed→completed; `test_timeout_cancel_dispatch_succeeded_for_file_change` canceled+any→canceled; both with side-effect uniqueness via counting wrappers). 2 `_TASK_19_FINALIZER_GUARD_REASON` constant declarations deleted. 3 L9 warning-discipline tests added (tombstone, anomalous-pending with L2-specific substring, parse-failed pending silence). 8 L11 direct finalizer-guard tests added (T1-T7b in new `tests/test_finalize_turn_terminal_guard.py`). All G18.1 tests required `session.respond` stubbing — `_FakeSession` and `_ConfigurableStubSession` default `respond=None` causes `TypeError` without it.

  **Closeout-fix (`4409b23c`):** 4 review-driven fixes: (1) F16.1 `if outcomes_path.exists():` → `assert outcomes_path.exists()` + side-effect uniqueness counting wrappers for `session.close()`, `registry.release()`, `lineage_store.update_status()` (L8.2 compliance); (2) L11-T7b `get_call_count >= 1` → `== 2` (derivation + hydration); (3) missing `assert final_job.status == "completed"` after bounded-poll in deny-emits-terminal-outcome; (4) triple JSON parsing → pre-parse once.

  **Carry-forward closures:** G18.1 CLOSED (all 6 tests unskipped). F16.1 CLOSED (both tests unskipped with concrete bodies). F16.2 CLOSED (lineage marker — G18.1 landed; lineage behavior verified via L11-T2 unknown→lineage completed and F16.1 tests). TT.1 UNCHANGED. RT.1 UNCHANGED.

  **Verification:** 1040 passed / 0 skipped / 0 failed (pre-Task-19: 1020 passed / 8 skipped). Census: 1020 + 12 new (L11+L9) + 8 unskipped (G18.1+F16.1) = 1040. Wall-clock ~250s. Ruff lint clean on touched files (3 pre-existing F401 in untouched files). All locks L1-L11 and watchpoints W1-W12 verified via grep audits. `DelegationEscalation(` count = 2, `_TASK_19_FINALIZER_GUARD_REASON` count = 0, `_decided_request_ids` count = 0, `resolution.payload.get` count = 0, `@pytest.mark.skip` in handler-branches = 0.

---

## How to add an item

Append to the appropriate "Open items" subsection with:
- **#**: stable identifier (phase-letter + task number + sequence, e.g., `B6.3`)
- **Item**: one-sentence description of the finding
- **Lands at / How to resolve**: natural resolution point
- **Source**: where it was found (review, dispatch, etc.)

When resolving, move the row to "Closed items" and append the resolving commit SHA in brackets.
