---
description: Resume from a previous handoff document
argument-hint: [path]
---

**Action:** Use the Skill tool to invoke the `handoff` skill, then follow its **resume procedure** (section: "Resuming from Handoff" → "Manual (`/resume`)").

If `$ARGUMENTS` is provided, pass it as the path to load.
