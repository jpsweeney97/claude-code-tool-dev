# context-injection

Canonical `context-injection` package for the cross-model plugin.

- Code: `context_injection/`
- Tests: `tests/`
- Contract: `packages/plugins/cross-model/references/context-injection-contract.md`

## Commands

```bash
uv run pytest
uv run ruff check context_injection/ tests/
python -m context_injection
```
