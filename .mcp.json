{
  "mcpServers": {
    "context-injection": {
      "type": "stdio",
      "command": "uv",
      "args": ["run", "--directory", "packages/context-injection", "python", "-m", "context_injection"],
      "env": {
        "REPO_ROOT": "${PWD}"
      }
    }
  }
}
